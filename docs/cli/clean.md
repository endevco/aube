---
description: "Remove node_modules across every workspace project."
editLink: false
---

<!-- @generated by usage-cli from usage spec -->
# `aube clean`

- **Usage:** `aube clean [-l --lockfile]`
- **Effect:** modifies state

Remove `node_modules` across every workspace project.

`--lockfile` / `-l` also deletes lockfiles. A `clean` script in the root `package.json` overrides the built-in.

<!-- docs-navigation:start -->

## Examples

```sh
aube clean
```

Read the [workflow guide](/package-manager/dependencies) for context.
[Global options](/cli/#global-flags) apply to this command too.

<!-- docs-navigation:end -->

## Flags

- **`-l --lockfile`** — Also remove lockfiles at the workspace root.

  Targets `aube-lock.yaml`, `pnpm-lock.yaml`, `package-lock.json`, `npm-shrinkwrap.json`, `yarn.lock`, and `bun.lock`.
- **`-h --help`** — Print help
