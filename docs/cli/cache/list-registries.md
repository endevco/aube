---
description: "List configured registries from the project + user .npmrc."
editLink: false
---

<!-- @generated by usage-cli from usage spec -->
# `aube cache list-registries`

- **Usage:** `aube cache list-registries`
- **Effect:** read-only

List configured registries from the project + user `.npmrc`.

aube stores all packuments in a single flat directory (unlike pnpm's per-host layout), so this prints the registries you're currently configured to talk to rather than the registries that happen to be in the cache.

<!-- docs-navigation:start -->

## Examples

```sh
aube cache list-registries
```

Read the [workflow guide](/package-manager/registry-auth) for context.
[Global options](/cli/#global-flags) apply to this command too.

<!-- docs-navigation:end -->

## Flags

- **`-h --help`** — Print help
