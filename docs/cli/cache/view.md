---
description: "View the cached metadata for a single package."
editLink: false
---

<!-- @generated by usage-cli from usage spec -->
# `aube cache view`

- **Usage:** `aube cache view [--json] <NAME>`
- **Effect:** read-only

View the cached metadata for a single package.

Prints a summary (versions, dist-tags, ETag, fetched-at) by default; `--json` dumps the raw cache file.

<!-- docs-navigation:start -->

## Examples

```sh
aube cache view react --json
```

Read the [workflow guide](/package-manager/registry-auth) for context.
[Global options](/cli/#global-flags) apply to this command too.

<!-- docs-navigation:end -->

## Arguments

- **`<NAME>`** — Package name (scoped names like `@babel/core` are accepted).

## Flags

- **`--json`** — Dump the raw on-disk cache JSON instead of a summary.
- **`-h --help`** — Print help
