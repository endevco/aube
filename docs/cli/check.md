---
description: "Verify installed packages can resolve their declared deps."
editLink: false
---

<!-- @generated by usage-cli from usage spec -->
# `aube check`

- **Usage:** `aube check [--json]`
- **Effect:** read-only

Verify installed packages can resolve their declared deps.

Walks the `node_modules/` symlink tree and confirms every dependency in each `package.json` resolves to a real entry.

<!-- docs-navigation:start -->

## Examples

```sh
aube check --json
```

Read the [workflow guide](/troubleshooting) for context.
[Global options](/cli/#global-flags) apply to this command too.

<!-- docs-navigation:end -->

## Flags

- **`--json`** — Emit a JSON report instead of the human-readable list.
- **`-h --help`** — Print help
