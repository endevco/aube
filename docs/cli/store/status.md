---
description: "Verify the store against cached package indexes."
editLink: false
---

<!-- @generated by usage-cli from usage spec -->
# `aube store status`

- **Usage:** `aube store status`
- **Effect:** read-only

Verify the store against cached package indexes.

Confirms every file referenced by a cached package index is still present in the store and that its BLAKE3 hash matches. Exits non-zero when any corruption is detected.

<!-- docs-navigation:start -->

## Examples

```sh
aube store status
```

Read the [workflow guide](/package-manager/node-modules) for context.
[Global options](/cli/#global-flags) apply to this command too.

<!-- docs-navigation:end -->

## Flags

- **`-h --help`** — Print help
